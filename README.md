# neuralnet-from-scratch
A neural network framework built from scratch in NumPy — supports dense, convolutional, pooling, and flatten layers with multiple optimizers (SGD, Momentum, RMSProp, Adam) and activation functions.
